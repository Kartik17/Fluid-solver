''' This file is used to create all the initial matrixes for temperature distribution across
    the entire grid at t = 0'''

import numpy as np

sizeX = 10
sizeY = 10

tempA = np.zeros(shape=(sizeX,sizeY))
tempX = np.zeros(shape=(sizeX,sizeY))

print tempA
